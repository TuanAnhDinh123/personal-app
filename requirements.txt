# Giao diện (PySide6) — chạy bằng: python main.py
PySide6>=6.6         # Qt hiện đại (light + sidebar tối), style bằng app_qt/theme.qss

# Logic nghiệp vụ (app/core/*):
openpyxl>=3.1         # Đọc/ghi .xlsx (Tách lương, Thưởng quý, Quét CV, các tool DB)
pymupdf>=1.24         # PDF → Text & Quét CV: trích text, dò bảng, render trang
pywin32>=306           # Đọc lịch & gửi mail qua Outlook (chỉ Windows)

# OCR cho PDF scan đi qua Gemini (chỉ dùng urllib chuẩn) — không cần cài thêm
# gói nào, cũng không cần quyền admin. API key đặt ở màn hình Settings.
