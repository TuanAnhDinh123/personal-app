# Giao diện (PySide6) — chạy bằng: python main.py
PySide6>=6.6         # Qt hiện đại (light + sidebar tối), style bằng app_qt/theme.qss
pillow>=10.0         # dựng ảnh cho OCR (PDF → Text)

# Logic nghiệp vụ (app/core/*):
# pandas>=2.0          # Gộp Excel, Làm sạch dữ liệu
openpyxl>=3.1         # Đọc/ghi .xlsx (Gộp Excel, Quét CV → xuất bảng tổng hợp)
# pypdf>=4.0           # Gộp / Tách PDF
pymupdf>=1.24         # PDF → Text & Quét CV: trích text từ PDF
pytesseract>=0.3.10   # OCR cho PDF scan (cần cài thêm Tesseract-OCR + gói tiếng Việt)
pywin32>=306           # Đọc lịch & gửi mail qua Outlook (chỉ Windows)
